#include "main.h"
#include "_putchar.c"
/**
 * _puts - print string followed by new line
 *
 * @str: prints strings
 */
void _puts(char *str)
{
int i;
for (i = 0; str[i] != '\0'; i++)
{
_putchar(str[i]);
}
_putchar('\n');
}
