#include <stdio.h>

/**
 * main - The main program prints out a string using printf;
 *
 * Return: Main is always (0) Success;
 */

int main(void)
{
printf("with proper grammar, but the outcome is a piece of art, \n");
return (0);
}
